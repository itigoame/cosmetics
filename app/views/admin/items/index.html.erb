<%= link_to "投稿", new_admin_item_path %>

<%= render "map" %>

<% @items.each do |item| %>
  <%= item.name %>
  <%= item.body %>
  <%= item.price %>
  <%= image_tag item.image,size: "30x30" %>
<% end %>